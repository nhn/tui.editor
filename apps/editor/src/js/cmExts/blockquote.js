'use strict';

var CodeMirror = window.CodeMirror;

var Blockquote = {
    name: 'Blockquote',
    type: 'md',
    fn: function blockquote(cm) {
        
    },
    keyMap: ['Ctrl-Q', 'Cmd-Q']
};

module.exports = Blockquote;
