var Selection = require('../src/js/selection');

describe('Selection', function() {
    'use strict';

    var sel;

    beforeEach(function() {
        /*
        jasmine.getFixtures().fixturesPath = '/base/test/fixtures';
        loadFixtures('selection.html');

        sel = new Selection({
            $editorEl: $('pre')
        });
        */
    });

    describe('현재 위치를 텍스트영역상의 위치값으로 저장한다.', function() {
        it('', function() {
            //console.log($('pre')[0].innerText);
            expect({}).toBeDefined();
        });
    });
});
